# Screenshots

Placeholders referenced from the main README. Capture them from the dashboard
at `http://127.0.0.1:5000` after running the demo walkthrough:

| File | What to capture |
|---|---|
| `01-import.png` | Step 1 after extracting `experiments/sample_chat.txt` with redaction on |
| `02-review.png` | Step 2 showing categories, confidence bars, a source excerpt, and a redacted badge |
| `03-retrieve.png` | Step 3 with the query "database schema decisions", showing scores, matched terms, and token savings |
| `04-export.png` | Step 4 with a Claude-formatted prompt scoped to the retrieval selection |
| `05-merge.png` | Merge preview with a folded duplicate and a flagged conflict |
| `demo.gif` | The four steps end to end (15–20 seconds) |

Keep images under 400 KB (PNG, 1280px wide) so the repository stays light.
